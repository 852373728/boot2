package com.sun;

import lombok.Data;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class UserContext {
     final static Log logs = LogFactory.getLog(UserContext.class);
     final Log logf = LogFactory.getLog(getClass());


}
